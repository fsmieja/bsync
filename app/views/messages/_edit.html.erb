<%= form_for @message do |f| %>

	<%= f.label 'Title' %>	
	<%= f.text_field :title %><br/>
	<%= f.label 'Content' %>	
	<%= f.text_area :content %><br/>
	<%= f.submit %>
<%= hidden_field_tag "id", @project.id %>
	
<% end %>
