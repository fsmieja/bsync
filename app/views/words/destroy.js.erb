var href = '/words/<%= @id %>/edit';
// remove the row
$('tr').find('a[href="' + href + '"]').parents("tr").hide();
var numVisible = $('tr:visible').length;
if (numVisible == 0) {
	$('table').hide();
}
